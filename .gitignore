# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment — .env.example is the committed template
.env*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Backup files and local asset archives
_*-backup/
*.bak
*.backup
*.orig
*.tmp

# Cursor IDE machine-local state (we track `.cursor/settings.json` for cross-repo plugin parity — see AGENTS.md)
.cursor/hooks/state/

# Machine-local file variants — named *-Mikes-PC.* on this workstation; never commit
*-Mikes-PC*

# Claude Code local config, permissions, and worktrees — not project source
.claude/

# Agent skill bundles populated by Claude Code / Hermes sync
.agents/

# Firebase local runtime cache (not firebase.json / .firebaserc)
.firebase/

# Brainstorming companion mockups (ephemeral design scratch space)
.superpowers/
